{
  "name": "briggs-empire",
  "version": "1.0.0",
  "description": "The Adventures of Briggs - AI Publishing Empire",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma generate && npx prisma db push && node index.js",
    "build": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "openai": "^4.0.0",
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "prisma": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
